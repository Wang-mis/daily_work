``` bash
grep [参数] "模式" [文件名/目录名]
```
从文件或标准输入中正则匹配。
## 参数
- 匹配相关
	- -i   忽略大小写
	- -v  反向查找
	- -w 查找完整单词
- 显示相关
	- -c 只显示匹配行数
	- -n 显示内容的同时显示行号
	- -l  只显示内容中存在匹配文本的文件的文件名
	- -A x 显示匹配行之前x行的内容
	- -B x 显示匹配行之后x行的内容
	- -C x 显示匹配行前后x行的内容
- -r 递归文件夹下的所有文件
- -E 现代正则匹配，支持更现代的正则语法。`grep -E`等价于`egrep`。
